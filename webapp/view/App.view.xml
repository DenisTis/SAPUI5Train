<mvc:View
  xmlns:mvc="sap.ui.core.mvc"
  xmlns:core="sap.ui.core"
  xmlns="sap.m" controllerName="companyRepo.appName.controller.App" displayBlock="true">
  <App>
    <Page title="{i18n>Header}">
      <content>
        <List headerText="Product Details" items="{/Products}">
          <items>
            <ObjectListItem title="{ProductName}" number="{ProductID}" intro="{QuantityPerUnit}"/>
          </items>
        </List>

  <!-- <Table id="PZaccountsTable2"
    items="{mainJSON>/accounts}">
    <columns>
      <Column width="11rem">
        <Text text="posNo"/>
      </Column>
      <Column width="11rem">
        <Text text="currency"/>
      </Column>
    </columns>      
    <items>
      <ColumnListItem>
        <cells>
          <Text text="{mainJSON>posNo}" /> 
          <Text text="{mainJSON>currency}" />           
        </cells>
      </ColumnListItem>
     </items> 
  </Table>   -->

      </content>
    </Page>
  </App>
</mvc:View>
